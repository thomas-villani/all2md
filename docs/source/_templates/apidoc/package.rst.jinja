{{ pkgname | escape | heading }}

.. automodule:: {{ pkgname }}
{%- for option in automodule_options %}
   :{{ option }}:
{%- endfor %}
   :imported-members: False
   :noindex:

{# Note: Toctrees are intentionally omitted to avoid duplicate references with #}
{# the hand-crafted conceptual organization files (core.rst, cli.rst, parsers.rst, etc.) #}
{# that already provide proper navigation to subpackages and submodules. #}
