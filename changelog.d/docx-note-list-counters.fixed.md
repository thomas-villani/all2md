- **DOCX: a numbered list that runs from one footnote into the next keeps counting.** It
  restarted at `1.` in the second note where Word prints `3.`, `4.`. Footnotes and
  endnotes count their lists separately from the document body, as Word does.
